# MODULE 7: CHECK ON KNOWLEDGE

#### Perform the following challenge.

- Create a CLI tool that:
  - Reads a folder of `.md` files
  - Generates a report of word counts, titles, and missing metadata
  - Outputs the summary to a `docs_report.md`


#### 
